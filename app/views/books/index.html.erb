<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Body</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <% @books.each do |book| %>
    <tr>
      <td>投稿ユーザー画像</tb>
      <td><%= book.title %></td>
      <td><%= book.opinion %></td>
    </tr>
    <% end %>
  </tbody>
</table>

<h1>New Book</h1>
<%= form_with model: @book, local:true do |f| %>
  <h4>Title</h4>
    <%= f.text_field :title %>
  <h4>Opinion</h4>
    <%= f.text_field :opinion %>
  <%= f.submit 'Create Book' %>
<% end %>