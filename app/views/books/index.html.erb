<% if @book.errors.any? %>
  <div id="error_explanation">
        <h2>
          <%= @book.errors.count %> errors prohibited this book from being saved
        </h2>
        <ul>
          <% @book.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
  </div>
<% end %>
<table>
  <thead>
    <tr>
      <th>image</th>
      <th>name</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <% @books.each do |book| %>
    <tr>
      <td>投稿ユーザー画像:<%= image_tag book.user.profile_image("100x100") %></tb>
      <td>
        <%= link_to book_path(book.id) do %>
          <%= book.title %>
        <% end %>
      </td>
      <td><%= book.body %></td>
    </tr>
    <% end %>
  </tbody>
</table>

<h1>New Book</h1>
<%= form_with model: @book, local:true do |f| %>

  <h4>Title</h4>
    <%= f.text_field :title %>
  <h4>Opinion</h4>
    <%= f.text_field :body %>
  <%= f.submit 'Create Book' %>
<% end %>